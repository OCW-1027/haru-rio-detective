# 하루게임 채팅 — 묶음 A: i18n(일본어화) + 이모티콘

## 0. 컨텍스트

Phase 1 (텍스트 채팅 + 페어링) 배포 완료 (commit `77458a2`).

자녀 하루는 일본어 모국어이므로 모든 UI 텍스트를 일본어로 전환.
추가로 카톡 스타일 이모지 입력 기능 도입.

- 작업 폴더: `C:\Users\taise\Projects\harugame`
- 브랜치: `main`
- 무료 플랜 그대로 (Spark, 비용 발생 없음)

---

## 1. 작업 범위

작은 작업 (예상 1~2시간).

1. `engine/chat-ui.js`의 모든 한국어 UI 텍스트 → 일본어 변경
2. `engine/chat-core.js`의 호칭 기본값 변경: "아빠"/"하루" → "パパ"/"ハル"
3. 이모지 입력 기능 추가 (입력창 옆 😀 버튼 + 패널)
4. `styles/chat.css`에 이모지 패널 스타일 추가

기존 게임 코드, Firestore 데이터 모델, 보안 규칙, Firebase 설정 — **전부 무수정**.

---

## 2. 일본어 번역 매핑 (실제 코드에서 검색·치환)

### 2-1. 역할 선택 화면

| 한국어 | 일본어 |
|---|---|
| 역할 선택 | 役割選択 |
| 채팅 시작 | チャットを始める |
| 당신은 누구인가요? | あなたはどちらですか？ |
| 내가 부모 | 私は親(パパ) |
| 내가 자녀(하루) | 私は子(ハル) |

### 2-2. 페어링 화면 (부모)

| 한국어 | 일본어 |
|---|---|
| 페어링 코드 만들기 | ペアリングコードを作る |
| 5분 안에 자녀 기기에서 입력하세요 | 5分以内に子の機器で入力してください |
| 자녀의 페어링을 기다리는 중... | 子のペアリングを待っています… |
| 코드 다시 만들기 | コードを作り直す |

### 2-3. 페어링 화면 (자녀)

| 한국어 | 일본어 |
|---|---|
| 페어링 코드 입력 | ペアリングコードを入力 |
| 6자리 코드를 입력하세요 | 6桁のコードを入力してください |
| 확인 | 確認 |
| 코드를 찾을 수 없습니다 | コードが見つかりません |
| 코드가 만료되었습니다 | コードの有効期限が切れました(5分超過) |
| 페어링 실패 | ペアリングに失敗しました |

### 2-4. 채팅 화면

| 한국어 | 일본어 |
|---|---|
| 닫기 | 閉じる |
| 메시지 입력... | メッセージを入力… |
| 전송 | 送信 |

### 2-5. 설정 화면

| 한국어 | 일본어 |
|---|---|
| 설정 | 設定 |
| 부모 호칭 | 親の呼び方 |
| 자녀 호칭 | 子の呼び方 |
| 호칭 저장 | 呼び方を保存 |
| 저장됨 | 保存しました |
| 돌아가기 | 戻る |
| 페어링 초기화 | ペアリングをリセット |
| 정말 페어링을 초기화하시겠습니까? | 本当にペアリングをリセットしますか？ |

### 2-6. 기타 (로딩·에러)

| 한국어 | 일본어 |
|---|---|
| 로딩 중... | 読み込み中… |
| 페어링이 필요합니다 | ペアリングが必要です |
| 페어링이 끊어졌습니다 | ペアリングが切断されました |
| 다시 시작 | 再度開始 |

**중요**: chat-ui.js 안의 모든 한국어 문자열을 빠짐없이 검색해서 일본어로 치환.
정규식 검색: `/[가-힣]/` 으로 미번역 한국어 잔존 여부 확인 필수.

---

## 3. 호칭 기본값 변경

`engine/chat-core.js`의 ChatState 초기값 및 KEY 폴백:

```
ChatState.parentName: '아빠' → 'パパ'
ChatState.childName: '하루' → 'ハル'

localStorage 폴백:
localStorage.getItem(KEY.PARENT_NAME) || 'アバ' → || 'パパ'
localStorage.getItem(KEY.CHILD_NAME) || '하루' → || 'ハル'
```

### 3-1. 기존 사용자 처리 (마이그레이션)

이미 사용 중인 부모 PC에는 localStorage에 "아빠"/"하루"가 저장되어 있음.

**처리 방침**: 자동 마이그레이션 안 함. 기존 값 그대로 유지.
- 사용자가 설정 화면에서 직접 변경하면 됨
- 신규 디바이스(자녀 태블릿 5/15)는 일본어 기본값으로 시작

이유: 사용자가 한국어 호칭으로 굳이 두고 싶을 수도 있고, 자동 변경하면 사용자 의도와 다를 수 있음.

---

## 4. 이모지 입력 기능

### 4-1. UI 레이아웃 변경

기존:
```
┌────────────────────────────────────┐
│ [메시지 입력창........] [전송]       │
└────────────────────────────────────┘
```

변경 후:
```
┌────────────────────────────────────┐
│ [😀] [メッセージを入力…........] [送信]│
└────────────────────────────────────┘
```

- 😀 이모지 버튼: 입력창 좌측에 정사각형 (44x44px, 충분한 터치 크기)
- 클릭 시 입력창 위로 이모지 패널이 슬라이드 업
- 패널 닫기: 😀 버튼 다시 클릭 또는 입력창 포커스 시

### 4-2. 이모지 패널 구조

2단:

**상단 — 빠른 선택 (12개, 자주 쓰는 것)**
```
😀 😂 😍 🥰 😊 😢 😭 ❤️ 👍 🎉 🌟 ✨
```

**하단 — 카테고리별 전체 (4 탭)**

탭 라벨: `表情` / `動物` / `食べ物` / `ハート`

각 탭의 이모지 (그리드 레이아웃, 6열 정도):

#### 表情 (얼굴, 약 28개)
😀 😃 😄 😁 😆 😂 🤣 😊 😇 🙂 🙃 😉 
🥲 🥰 😍 🤩 😘 😗 😚 😙 🤗 🤔 🤨 😐 
🙄 😏 😴 🤤 😪 😫 🥱 😴 😌 😛 😜 🤪 
😎 🤓 🥺 😢 😭 😤 😠 😡 🤯 😱 😨 😰

#### 動物 (동물·자연, 약 28개)
🐶 🐱 🐭 🐹 🐰 🦊 🐻 🐼 🐨 🐯 🦁 🐮 
🐷 🐽 🐸 🐵 🙈 🙉 🙊 🐔 🐧 🐦 🐤 🦄 
🐝 🐛 🦋 🐌 🐞 🐢 🐬 🐳 🌸 🌺 🌻 🌷 
🌹 🌼 🌳 🌲 🍀 🌟 ⭐ ☀️ ⛅ ☁️ 🌙 ⚡

#### 食べ物 (음식, 약 24개)
🍎 🍌 🍓 🍇 🍉 🍊 🍒 🍑 🥝 🍍 🥭 🥑 
🍕 🍔 🍟 🌭 🌮 🍿 🍰 🧁 🍪 🍩 🍫 🍬 
🍭 🍮 🍦 🍡 🍙 🍚 🍜 🍣 🍤 🍱 🍛 🍝 
☕ 🍵 🥛 🧃 🥤

#### ハート (하트·기호, 약 24개)
❤️ 🧡 💛 💚 💙 💜 🖤 🤍 💔 ❤️‍🔥 💕 💖 
💗 💘 💝 💞 💟 ✨ 🌟 ⭐ 🎉 🎊 🎈 🎁 
🎀 💯 🔥 ⚡ 👍 👎 🙏 👋 ✌️ 👌 🤝 🤲

### 4-3. 동작 사양

- 빠른 선택 또는 카테고리 이모지 클릭 → 입력창 텍스트 끝에 추가
- 패널은 자동으로 닫히지 않음 (연속 선택 가능)
- 입력창 포커스 시 패널 자동 닫기
- 텍스트 + 이모지 혼합 입력 OK (예: "おはよう🌞")
- 전송 시 이모지 그대로 Firestore에 저장 (UTF-8)
- 받은 이모지 메시지 정상 렌더링 (브라우저 기본 이모지 폰트)

### 4-4. 큰 폰트·터치 영역으로 가독성·조작성 확보

- 빠른 선택 12개는 항상 보이게 (자주 쓰는 것 즉시 접근)
- 카테고리 탭은 큰 터치 영역 (44px+)
- 이모지 그리드 셀: 최소 44x44px, 충분한 간격 (gap 8px)
- 활성 탭은 명확한 색상 구분 (배경색 변화)

### 4-5. CSS 클래스 (chat.css 추가, .chat- 프리픽스 유지)

```
.chat-emoji-btn          입력창 옆 😀 버튼
.chat-emoji-panel        패널 컨테이너
.chat-emoji-quick        빠른 선택 영역
.chat-emoji-tabs         카테고리 탭 영역
.chat-emoji-tab          개별 탭
.chat-emoji-tab-active   활성 탭
.chat-emoji-grid         이모지 그리드
.chat-emoji-item         개별 이모지 셀
```

---

## 5. 변경 파일

신규: 0개  
수정: 3개 파일

```
engine/chat-ui.js        UI 텍스트 일본어화, 이모지 패널 렌더링·이벤트
engine/chat-core.js      호칭 기본값 'パパ'/'ハル'로
styles/chat.css          이모지 버튼·패널 스타일 추가
```

명세 문서:
```
CHAT_BUNDLE_A_I18N_EMOJI_SPEC.md   (이 문서, 신규 추가)
```

---

## 6. 검증

1. 브라우저 강제 새로고침 (Ctrl+Shift+R)
2. 모든 화면 텍스트가 일본어로 표시되는지 확인
   - 역할 선택 / 페어링 / 채팅 / 설정 / 에러 메시지 전부
3. 입력창 좌측에 😀 버튼 보임
4. 클릭 시 이모지 패널 펼쳐짐 (애니메이션 자연스러움)
5. 빠른 선택 12개 이모지 표시
6. 카테고리 탭 4개(表情/動物/食べ物/ハート) 전환 가능
7. 이모지 클릭 → 입력창에 추가됨
8. 이모지 여러 개 + 텍스트 혼합 입력 OK
9. 전송 → 상대방 메시지 리스트에 정상 표시 (이모지 깨짐 없음)
10. 안드로이드 Chrome (가능하면 자녀 태블릿) 에서도 동작 확인

검증 통과 못하면 수정 후 재검증.

---

## 7. Git 작업

모든 검증 통과 후 한 번에:

```bash
git add engine/chat-ui.js engine/chat-core.js styles/chat.css CHAT_BUNDLE_A_I18N_EMOJI_SPEC.md
git commit -m "feat(chat): i18n to Japanese + emoji picker (Bundle A)

- All UI strings translated to Japanese (役割選択/ペアリング/設定 etc.)
- Default names: パパ / ハル (existing localStorage preserved)
- Emoji picker beside input: 12 quick-select + 4 category tabs
  (表情·動物·食べ物·ハート, ~100 emojis total)
- Large touch targets (44px+), persistent quick-select row
- No backend/security/data-model changes (UI-only)"
git push origin main
```

---

## 8. 다음 단계 (이번 작업 후)

- 묶음 B (Phase 3): presence·타이핑·읽음·in-app 알림 — Firestore만 사용, 무료
- 묶음 C (Phase 4): PWA + Service Worker + FCM 푸시 — Blaze 업그레이드 필요

각 묶음마다 별도 명세서(`CHAT_BUNDLE_B_*.md`, `CHAT_BUNDLE_C_*.md`)로 진행.
